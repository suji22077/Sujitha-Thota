#include <stdio.h>
int main()
{
    int n,p,a,b;
    scanf("%d",&n);
    scanf("%d",&p);
    a=(p/2);
    b=(n/2)-(p/2);
    if(a<b)
    {
     printf("%d",a);
    }
    else 
    {
        printf("%d",b);
    }
    return 0;
}
